list ? 
a list is a set of daa which is - 
indexed, 
mutable 
slicing also works the same way as string

some list functions include 
.append()
.sort
.find(element)
.remove(element)
.pop() removes last elements
.reverse()
.max()
.min()
.sum() #if integer

numpy funcitons and their uses.
| Function     | Work           |
| ------------ | -------------- |
| np.array()   | Create array   |
| np.zeros()   | Array of zeros |
| np.ones()    | Array of ones  |
| np.arange()  | Range array    |
| np.reshape() | Change shape   |
| a.shape      | Shape          |
| a.size       | Total elements |
| a.ndim       | Dimension      |
